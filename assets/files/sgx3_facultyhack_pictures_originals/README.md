Full-resolution backups of the photos in `assets/files/sgx3_facultyhack_pictures/`,
kept here in case the web-optimized working copies ever need to be re-derived
at a different size. Excluded from the Jekyll build in `_config.yml` — nothing
in this folder is served on the site.
